[project]
name = "odisseus-raspberry-pi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexgiavaras", email = "alex.giavaras@trilodocs.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pyinfra>=3.10.0",
]

[project.scripts]
odisseus-raspberry-pi = "odisseus_raspberry_pi:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
